server:
  port: PORT NUMBER HERE

spring:
  application:
    name: YOUR APPLICATION NAME EG- ORDER-SERVICE




eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/


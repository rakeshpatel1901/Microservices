application.properties 

spring.datasource.url=jdbc:mysql://localhost:3306/mahalaxmi_orders
spring.datasource.username=DATABASE_ROOT
spring.datasource.password=DATABASE_PASSWORD

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true



spring.cloud.stream.bindings.orderPlacedNotification-out-0.destination=order.created
spring.cloud.stream.bindings.orderPlacedNotification-out-0.content-type=application/json
spring.cloud.stream.bindings.orderPlacedNotification-out-0.group=${spring.application.name}

spring.cloud.stream.bindings.orderPlacedNotification-in-0.destination=order.ack
spring.cloud.stream.bindings.orderPlacedNotification-in-0.content-type=application/json
spring.cloud.stream.bindings.orderPlacedNotification-in-0.group=${spring.application.name}


application.yml


server:
  port: 8082

spring:
  application:
    name: ORDER-SERVICE




eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

